# 🚀 快速入门指南

> 新手友好的 ESP32-S3 433MHz 智能遥控器入门教程

---

## 📦 准备工作

### 你需要准备的东西

#### 硬件清单
- ✅ 立创 ESP32-S3-R8N8 开发板 × 1
- ✅ 远-T2L_433 发射模块 × 1
- ✅ 灵-R1A-M5_433 接收模块（串口版本）× 1
- ✅ USB Type-C 数据线 × 1
- ✅ 杜邦线若干（公对母）
- ✅ LED 指示灯 × 1（可选）
- ✅ 220Ω 电阻 × 1（可选，配合 LED）

> 💡 **完整套件清单**请查看 [HARDWARE_KIT.md](HARDWARE_KIT.md)

#### 软件准备
- 电脑（Windows / macOS / Linux）
- VS Code 编辑器
- 手机（用于连接 WiFi 和控制）

---

## 🔌 第一步：硬件连接

### 接线说明

按照下表连接模块到开发板：

| 模块 | 模块引脚 | → | 开发板引脚 |
|------|---------|---|-----------|
| 发射模块 | VCC | → | 5V |
| 发射模块 | GND | → | GND |
| 发射模块 | DATA | → | GPIO14 |
| 接收模块 | VCC | → | 5V |
| 接收模块 | GND | → | GND |
| 接收模块 | DATA | → | GPIO18 |
| LED（可选）| 正极（长脚）| → | GPIO21 |
| LED（可选）| 负极（短脚）| → | GND（需串联 220Ω 电阻）|

### 接线图示

```
ESP32-S3-R8N8 开发板
┌─────────────────────────────┐
│                             │
│  GPIO14 ──→ 发射模块 DATA   │
│  GPIO18 ──→ 接收模块 DATA   │
│  GPIO21 ──→ LED 正极        │
│                             │
│  5V     ──→ 模块 VCC        │
│  GND    ──→ 模块 GND + LED  │
│                             │
└─────────────────────────────┘
```

### ⚠️ 注意事项

1. **接收模块必须选择串口版本**（灵-R1A-M5_433），普通版本无法使用
2. 确保 VCC 和 GND 不要接反，否则可能烧毁模块
3. 连接前请断开开发板电源
4. LED 需要串联电阻，否则可能烧毁

---

## 💻 第二步：安装开发环境

### 方式一：PlatformIO（推荐新手）

#### 1. 安装 VS Code

1. 访问 [VS Code 官网](https://code.visualstudio.com/)
2. 下载对应系统的安装包
3. 安装并启动 VS Code

#### 2. 安装 PlatformIO 插件

1. 打开 VS Code
2. 点击左侧的 **扩展** 图标（或按 `Ctrl+Shift+X` / `Cmd+Shift+X`）
3. 搜索 `PlatformIO IDE`
4. 点击 **安装**
5. 安装完成后重启 VS Code

#### 3. 克隆项目

打开终端（Terminal），执行以下命令：

```bash
# 克隆项目到本地
git clone https://github.com/zhoushoujianwork/433_test_arduino.git

# 进入项目目录
cd 433_test_arduino
```

#### 4. 打开项目

1. 在 VS Code 中：`文件` → `打开文件夹`
2. 选择刚才克隆的 `433_test_arduino` 文件夹
3. 点击 **打开**

---

### 方式二：Arduino IDE

#### 1. 安装 Arduino IDE

1. 访问 [Arduino 官网](https://www.arduino.cc/en/software)
2. 下载 Arduino IDE 2.x 版本
3. 安装并启动

#### 2. 添加 ESP32 支持

1. 打开 Arduino IDE
2. 点击 `文件` → `首选项`
3. 在 **附加开发板管理器网址** 中添加：
   ```
   https://espressif.github.io/arduino-esp32/package_esp32_index.json
   ```
4. 点击 **确定**
5. 点击 `工具` → `开发板` → `开发板管理器`
6. 搜索 `ESP32`
7. 找到 `esp32 by Espressif Systems`，点击 **安装**

#### 3. 安装依赖库

1. 点击 `工具` → `管理库`
2. 搜索 `rc-switch`
3. 找到 `rc-switch by sui77`，点击 **安装**

#### 4. 打开项目

1. 下载项目 ZIP 文件并解压
2. 在 Arduino IDE 中打开 `src/main.cpp`

---

## 📤 第三步：上传代码到开发板

### 使用 PlatformIO

1. **连接开发板**
   - 用 USB Type-C 线连接开发板到电脑
   - 等待驱动自动安装（Windows 可能需要手动安装 CH340 驱动）

2. **编译项目**
   - 点击底部状态栏的 **✓** 图标（Build）
   - 等待编译完成（首次编译需要下载依赖，可能需要几分钟）

3. **上传代码**
   - 点击底部状态栏的 **→** 图标（Upload）
   - 等待上传完成

4. **查看串口输出**
   - 点击底部状态栏的 **🔌** 图标（Serial Monitor）
   - 查看设备启动日志

### 使用 Arduino IDE

1. **选择开发板**
   - 点击 `工具` → `开发板` → `ESP32 Arduino` → `ESP32S3 Dev Module`

2. **选择端口**
   - 点击 `工具` → `端口`
   - 选择对应的 COM 口（Windows）或 `/dev/cu.usbserial-xxx`（macOS）

3. **上传代码**
   - 点击 **上传** 按钮（→ 图标）
   - 等待上传完成

4. **查看串口输出**
   - 点击 `工具` → `串口监视器`
   - 设置波特率为 `115200`

---

## 📱 第四步：连接 WiFi 并使用

### 1. 连接 ESP32 热点

开发板上传代码后会自动重启，并创建 WiFi 热点：

1. 打开手机 WiFi 设置
2. 找到名为 `ESP433RF` 的 WiFi
3. 输入密码：`12345678`
4. 连接成功

> 💡 **自动跳转**：连接成功后，手机会自动弹出管理页面（Captive Portal），无需手动输入地址

> 💡 **提示**：手机可能提示"此网络无互联网连接"，点击"仍然连接"即可

### 2. 打开管理页面

如果没有自动弹出管理页面：

1. 打开手机浏览器（Safari / Chrome）
2. 访问地址：`http://192.168.4.1`
3. 看到管理页面即表示成功

### 3. 学习遥控器信号

现在可以开始学习你的遥控器信号了：

1. **点击"捕获信号"按钮**（橙色按钮）
2. **按下遥控器按键**（距离接收模块 10-30cm）
3. **信号自动保存**到列表最上方
4. 重复以上步骤可学习多个信号

### 4. 发送信号

学习完信号后，有三种方式发送：

#### 方式 1：网页发送
- 点击信号旁边的 **"发送"** 按钮（绿色）

#### 方式 2：绑定 Boot 按钮
1. 点击信号旁边的 **"绑定"** 按钮（橙色）
2. 该信号会显示 **"🎯 Boot 绑定"** 标签
3. 短按开发板上的 **Boot 按钮**即可发送

#### 方式 3：删除信号
- 点击信号旁边的 **"删除"** 按钮（红色）

---

## 🎮 Boot 按钮使用说明

开发板上的 Boot 按钮有两个功能：

| 操作 | 功能 | 说明 |
|------|------|------|
| **短按** | 发送绑定的信号 | 快速发送常用信号 |
| **长按 2 秒** | 清空复刻信号 | 清空旧的学习模式信号 |

---

## 💡 LED 指示灯说明

如果你连接了 LED，可以通过 LED 状态了解设备状态：

| LED 状态 | 含义 | 说明 |
|---------|------|------|
| **熄灭** | 待机状态 | 系统正常运行 |
| **快闪** | 捕获模式 | 等待接收信号 |
| **常亮** | 已捕获信号 | 信号学习成功 |

---

## ❓ 常见问题解决

### Q1: 上传代码失败？

**可能原因和解决方法：**

1. **端口选择错误**
   - 重新选择正确的 COM 口
   - 拔插 USB 线重试

2. **驱动未安装**（Windows）
   - 下载并安装 [CH340 驱动](http://www.wch.cn/downloads/CH341SER_EXE.html)
   - 重启电脑后重试

3. **开发板未进入下载模式**
   - 按住 Boot 按钮
   - 点击上传
   - 看到"Connecting..."后松开 Boot 按钮

### Q2: 手机连不上 WiFi？

**解决方法：**

1. 确认开发板已上电且程序正常运行
2. 查看串口输出，确认 WiFi 已启动
3. 手机提示"无互联网连接"时，点击"仍然连接"
4. 尝试关闭手机的移动数据
5. 重启开发板重试

### Q3: 打不开管理页面？

**解决方法：**

1. 确认已连接到 ESP32 的 WiFi（`ESP433RF`）
2. 尝试访问 `http://192.168.4.1`
3. 查看串口输出中显示的 IP 地址
4. 清除浏览器缓存后重试
5. 尝试使用其他浏览器

### Q4: 学习不到信号？

**解决方法：**

1. 确认接收模块连接正确（GPIO18）
2. 确认接收模块是**串口版本**（灵-R1A-M5_433）
3. 遥控器距离接收模块 10-30cm
4. 查看串口输出是否有接收日志
5. 确认遥控器是 433MHz 频率（可用手机 NFC 工具检测）

### Q5: 发送信号无效？

**解决方法：**

1. 确认发射模块连接正确（GPIO14）
2. 检查发射模块供电是否正常（建议 5V）
3. 发射距离不要太远（建议 1-5 米测试）
4. 查看串口输出确认是否发送成功
5. 尝试重新学习信号

### Q6: 串口输出乱码？

**解决方法：**

1. 确认波特率设置为 `115200`
2. 重新打开串口监视器
3. 重启开发板

---

## 🎯 下一步

恭喜你完成了快速入门！现在你可以：

- 🏠 学习家里所有 433MHz 遥控器的信号
- 🎮 将常用信号绑定到 Boot 按钮
- 🔧 修改 WiFi 名称和密码（参考 [readme.md](readme.md) 进阶配置）
- 📱 分享给朋友，让他们也能体验

---

## 📚 更多资源

- [项目主页](readme.md) - 查看完整文档
- [硬件套件清单](HARDWARE_KIT.md) - 购买完整套件
- [立创开发板文档](https://wiki.lckfb.com/zh-hans/esp32s3r8n8/) - 开发板详细资料
- [B 站视频演示](https://www.bilibili.com/video/BV12R19B5EsX/) - 观看视频教程

---

## 💬 需要帮助？

如果遇到问题，欢迎：
- 提交 [GitHub Issue](https://github.com/zhoushoujianwork/433_test_arduino/issues)
- 查看上面的"常见问题解决"章节
- 查看串口输出日志进行调试

---

<p align="center">
  祝你使用愉快！🎉<br>
  Made with ❤️ by Zhoushoujian
</p>
